### @ngInject ###
module.exports = ($scope, $modalInstance, title, body) ->

  $scope.title = title
  $scope.body = body

  $scope.yes = ->
    $modalInstance.close(true)

  $scope.no = ->
    $modalInstance.dismiss('cancel')
