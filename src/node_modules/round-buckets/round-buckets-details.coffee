module.exports = {
  url: '/:bucketId'
  template: require('./round-buckets-details-template.html')
  controller: require('./round-buckets-details-controller.coffee')
  resolve:
    bucket: (BucketService, $stateParams) ->
      BucketService.get($stateParams.bucketId)
}
