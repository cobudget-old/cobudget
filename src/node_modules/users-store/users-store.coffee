# ridiculous hack for @ngInject due to code at top from coffee class extension
null
### @ngInject ###
module.exports = (RestfulStore, UserModel) ->
  class UsersStore extends RestfulStore

  new UsersStore(
    singular: "user"
    plural: "users"
    Model: UserModel
  )