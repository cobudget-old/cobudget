module.exports = ->
  class UserModel
    constructor: (data = {}) ->
      @id = data.id
      @name = data.name
      @email = data.email
      @password = data.password

    serialize: ->
      return {
        id: @id
        name: @name
        email: @email
        password: @password
      }
