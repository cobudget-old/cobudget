module.exports = ->
  class UserModel
    constructor: (data = {}) ->
      @id = data.id
      @name = data.name
      @email = data.email

    serialize: ->
      return {
        id: @id
        name: @name
        email: @email
      }
