debug = require('debug')("error-modal")

### @ngInject ###
module.exports = ($scope, $modalInstance, error) ->

  debug("error", error)
  $scope.error = error

  $scope.close = ->
    $modalInstance.dismiss('close')
