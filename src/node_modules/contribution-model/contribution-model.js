/* @ngInject */
module.exports = function (BaseModel, UserModel) {
  return BaseModel.extend({
    modelType: "Contribution",

    props: {
      amount: 'decimal',
      bucketId: 'number',
    },

    children: {
      user: UserModel,
    },
  });
};
