// TODO: find a better solution than this massive hack

var extend = require('xtend')
var evaluate = require('./eval')

var defaults = require('../../../config/defaults.coffee')
var config = require(evaluate('../../../config/' + process.env.NODE_ENV + '.coffee'))

module.exports = extend(defaults, config, {
  env: process.env.NODE_ENV,
});
