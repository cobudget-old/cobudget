### @ngInject ###
module.exports = (options) ->
  template: require('./round-publish-modal-template.html')
  controller: require('./round-publish-modal-controller.coffee')
  size: 'lg'
  resolve:
    group: ->
      options.group
    round: ->
      options.round