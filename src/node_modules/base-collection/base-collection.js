var Collection = require('ampersand-collection');
var lodashMixin = require('ampersand-collection-lodash-mixin');

/* @ngInject */
module.exports = function (BaseModel) {
  return Collection.extend(lodashMixin, {
    model: BaseModel,
  });
};
