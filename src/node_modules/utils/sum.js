var Decimal = require('decimal');

module.exports = function sum (decimals) {
  return decimals.reduce(function accumulator (prev, curr) {
    if (!(curr instanceof Decimal)) {
      curr = new Decimal(curr);
    }
    return prev.plus(curr);
  }, new Decimal(0));
}
