# group page
module.exports = {
  url: '/groups/:groupId/home'
  template: require('./template.html')
  controller: require('./controller.coffee')
  #resolve:
  #  rounds: (RoundService, $stateParams) ->
  #    console.log($stateParams)
      #RoundService.all($stateParams.groupId)
}
