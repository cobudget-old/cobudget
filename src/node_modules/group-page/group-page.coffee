module.exports = {
  abstract: true
  url: '/groups/:groupId'
  template: require('./group-page-template.html')
  controller: require('./group-page-controller.coffee')
  resolve:
    group: (GroupService, $stateParams) ->
      GroupService.get($stateParams.groupId)
}
