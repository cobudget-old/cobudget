module.exports = {
  url: '/:bucketId'
  template: require('./details-template.html')
  controller: require('./details-controller.coffee')
  resolve:
    bucket: (BucketService, $stateParams) ->
      BucketService.get($stateParams.bucketId)
}
