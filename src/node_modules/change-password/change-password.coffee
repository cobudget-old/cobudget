module.exports = (options) ->
  template: require('./change-password-template.html')
  controller: require('./change-password-controller.coffee')
  size: 'lg'
  resolve:
    person: ->
      options.person
