module.exports = ($compile) ->
  (scope, element, attrs) ->
    if scope.banner.subtext
      subtextTmpl = "<span class='subtext'>#{scope.banner.subtext}</span>"
      subtextEl = $compile(subtextTmpl)(scope)
      element.find('.subtext').replaceWith(subtextEl)
