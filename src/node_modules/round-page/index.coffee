module.exports = {
  abstract: true
  url: '/groups/:groupId/rounds/:roundId'
  #url: '/groups/:groupId'
  template: require('./template.html')
  controller: require('./controller.coffee')
  resolve:
    group: (GroupService, $stateParams) ->
      GroupService.get($stateParams.groupId)
    #round: (RoundService, $stateParams) ->
    #  RoundService.get($stateParams.roundId)
}
