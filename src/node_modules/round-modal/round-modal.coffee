### @ngInject ###
module.exports = (options) ->
  template: require('./round-modal-template.html')
  controller: require('./round-modal-controller.coffee')
  size: 'lg'
  resolve:
    group: ->
      options.group
    round: ->
      options.round
