# ridiculous hack for @ngInject due to code at top from coffee class extension
null
### @ngInject ###
module.exports = (RestfulStore, config, BucketModel) ->
  class BucketsStore extends RestfulStore

  new BucketsStore(
    baseUrl: config.apiEndpoint
    singular: "bucket"
    plural: "buckets"
    Model: BucketModel
  )