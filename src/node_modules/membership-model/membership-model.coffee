### @ngInject ###
module.exports = (UserModel) ->
  class MembershipModel
    constructor: (data = {}) ->
      @id = data.id
      @groupId = data.groupId
      @user = new UserModel(data.user)

    serialize: ->
      {
        id: @id
        groupId: @groupId
        userId: @user.id
      }