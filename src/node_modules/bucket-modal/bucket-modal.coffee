### @ngInject ###
module.exports = (options) ->
  template: require('./bucket-modal-template.html')
  controller: require('./bucket-modal-controller.coffee')
  size: 'lg'
  resolve:
    group: ->
      options.group
    round: ->
      options.round
    # for some reason ngInject isn't working
    memberships: ['MembershipsStore', (MembershipsStore) ->
      MembershipsStore.all(groupId: options.group.id)
    ]
    bucket: ->
      options.bucket
