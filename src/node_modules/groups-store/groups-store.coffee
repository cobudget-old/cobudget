# ridiculous hack for @ngInject due to code at top from coffee class extension
null
### @ngInject ###
module.exports = (RestfulStore, config, GroupModel) ->
  class GroupsStore extends RestfulStore

  new GroupsStore(
    baseUrl: config.apiEndpoint
    singular: "group"
    plural: "groups"
    Model: GroupModel
  )