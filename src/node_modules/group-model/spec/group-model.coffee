expect = require('chai').expect
sinon = require('sinon')

GroupModelProvider = require('round-model')

RoundModel = sinon.stub()

describe "GroupModel", ->

  before ->
    @GroupModel = GroupModelProvider(RoundModel)

  it "should exist", ->
    expect(@GroupModel).to.exist
