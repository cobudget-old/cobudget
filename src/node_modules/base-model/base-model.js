var Model = require('ampersand-state');

/* @ngInject */
module.exports = function () {
  return Model.extend({

    idAttribute: 'id',
    typeAttribute: 'modelType',

    props: {
      id: 'number',
    },

  })
};
