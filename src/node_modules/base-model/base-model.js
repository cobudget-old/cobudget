var Model = require('ampersand-state');
var Decimal = require('big.js');

/* @ngInject */
module.exports = function () {
  return Model.extend({

    idAttribute: 'id',
    typeAttribute: 'modelType',

    props: {
      id: 'number',
    },

    dataTypes: {
      decimal: require('utils/decimal-type'),
      moment: require('utils/moment-type'),
    },
  });
};
