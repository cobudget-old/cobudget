/* @ngInject */
module.exports = function (BaseModel, UserModel) {
  return BaseModel.extend({
    modelType: "Allocation",

    props: {
      userId: "number",
      roundId: "number",
      amount: "decimal",
    },
  });
};
