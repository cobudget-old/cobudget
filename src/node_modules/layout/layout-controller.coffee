debug = require('debug')("layout")

### @ngInject ###
module.exports = ($scope, $state, authModel, groups) ->
  $scope.groups = groups || []

  authModel.on "login-success", ->
    debug("login success, fetching groups")
    $state.reload()

  authModel.on "logout-success", ->
    debug("logout success, clearing groups")
    $scope.groups = []
    $state.reload()
